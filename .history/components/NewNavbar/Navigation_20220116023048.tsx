import MenuItem from "./MenuItem"

const Navigation = () => {
    return (
        <li>
            [...Array(5)].map((_, i) ={">"}(
                <MenuItem/>
            ))
        </li>
    )
}

export default Navigation
